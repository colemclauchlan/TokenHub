{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the panel and mini-bar windows.",
  "windows": ["panel", "minibar"],
  "permissions": [
    "core:default"
  ]
}
